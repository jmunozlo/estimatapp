# OpenSpec Configuration — estimatapp
# Auto-generated by sdd-init on 2026-06-26
schema: spec-driven

context: |
  Tech stack: Python 3.13+ / FastAPI 0.128 / uvicorn / Jinja2 templates / vanilla JS / WebSockets (ws 16.0) / Pydantic 2.12
  Architecture: DDD (domain/application/infrastructure/routes layers), repository pattern with ABC interface, singleton in-memory persistence
  Testing: pytest 8+ / pytest-asyncio / httpx AsyncClient / pytest-cov 7+ / AsyncMock mocking / coverage fail_under 85%
  Style: ruff (100 cols, py313 target, strict rule set), ruff format
  Package manager: uv
  Deployment: Render (free plan, Python 3.13)
  Strict TDD: true (test runner detected, no opt-out marker)

rules:
  proposal:
    - Include rollback plan for risky changes (e.g. Supabase migration)
    - Evaluate effort vs. value for each of the 3 growth initiatives
  specs:
    - Use Given/When/Then for scenarios
    - Use RFC 2119 keywords (MUST, SHALL, SHOULD, MAY)
    - Separate spec per domain bounded context
  design:
    - Include sequence diagrams for complex flows (WebSocket + DB tx patterns)
    - Document architecture decisions with rationale in each ADR
    - Consider in-memory vs persistent dual-running during migration
  tasks:
    - Group by milestone: (1) Supabase, (2) Next.js, (3) Stripe, (4) Scale
    - Keep tasks completable in one session
    - Each task must reference its test file(s)
  apply:
    - Follow existing DDD patterns (domain → application → infrastructure)
    tdd: true
    test_command: "uv run pytest"
  verify:
    test_command: "uv run pytest --cov=app --cov-report=term-missing"
    build_command: "uv run ruff check . && uv run pytest"
    coverage_threshold: 85
  archive:
    - Warn before merging destructive deltas
    - Verify coverage still meets 85% threshold after merge
